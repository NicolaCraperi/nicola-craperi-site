import React from "react"
import { PagesContainer } from "../commons/styled"

const About = props => {
    return (
        <PagesContainer backgroundColor="red" padding={true}>
            <h1>ABOUT ME</h1>
        </PagesContainer>
    )
}

export default About
